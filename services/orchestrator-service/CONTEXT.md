# orchestrator-service — Context

> **Ubiquitous language for this bounded context.** Maintained by the `domain-model` skill; other
> skills read it. Vocabulary lives per-service, never at the repo root — two services may
> legitimately use the same word for different things.

## Terms

<!-- Populated by /domain-model. Format:
     | Term | Means | Does NOT mean | -->

## Boundaries

<!-- What this context owns, and where it hands off to another. Fill when the first
     cross-context confusion appears — an empty section is better than an invented one. -->
