/**
 * The tour's invented content, in one place.
 *
 * Shared so the six sections tell **one** story rather than six unrelated
 * screenshots: the task the plan card lists is the same task the daily list
 * carries and the same one the nudge notices. That continuity is what makes the
 * AI beat read as grounded rather than generic.
 *
 * Concrete enough to be recognisable, vague enough to age well.
 */
export const PLAN_TITLE = 'Learn microservices';

/** The task that keeps slipping — referenced by the plan, the list, and the nudge. */
export const GATEWAY_TASK = 'wire up the gateway';
